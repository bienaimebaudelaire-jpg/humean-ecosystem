# Runtime secrets and local data
.env
.env.*
!.env.example
*.local.json

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# Local capability observations may contain provider output
data/*.json
!data/*.example.json

# The real, locally-curated registry (copy schemas/capabilities.example.json
# to get started) — only the example is tracked in source control.
schemas/capabilities.json
